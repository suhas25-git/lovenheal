import React, { useState } from "react";
import { Link } from "react-router-dom";
import axios from "axios";

function loginform() {
  return <h2 className="title">otp varifications</h2>;
  <input
    className="input"
    type="number"
  />;
  <h2 className="title">response</h2>
}

export default loginform;
